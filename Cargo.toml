[package]
name = "firefly-launcher"
version = "0.1.0"
edition = "2021"

[dependencies]
firefly-rust = { path = "../firefly-rust", default-features = false, features = [
    "alloc",
] }
talc = { version = "4.4.0", default-features = false, features = ["lock_api"] }
